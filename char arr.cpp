#include<iostream>
//#include<string.h>
using namespace std;
class stack
{
	private:
		char arr[i];
		string_arr[i]={(a+b)+(c+d)}
	char 'c';
	public:
	void	push(char 'c' )
		{
			if(top=size-1)
			{
				cout<<"balanced"<<endl;
			}
			else
			cout<<"unbalanced"<<endl;
			char arr[i];
        	string_arr[i]={(a+b)+(c+d)}
        	for(int i<=n;i++)
			return 0;
		}
	void	pop()
		{
			
		}
};
int main()
{
/*	int n;
	char arr[i];
	string_arr[i]={(a+b)+(c+d)}
	for(int i<=n;i++)
	if(arr[i]=='(')
	{
		
	}*/
	
	//stack
}

